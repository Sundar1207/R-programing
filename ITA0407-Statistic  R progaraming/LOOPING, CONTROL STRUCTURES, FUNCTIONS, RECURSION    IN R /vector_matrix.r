# Create three vectors
a <- c(1, 2, 3)
b <- c(4, 5, 6)
c <- c(7, 8, 9)

# Combine them into a 3x3 matrix (column-wise)
matrix_3x3 <- cbind(a, b, c)

# Print the matrix
print(matrix_3x3)

#cbind(a, b, c) binds the vectors as columns to form a matrix.
